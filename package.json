{
  "name": "robinhood-chain-observer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check signals.js && node --check market-data.js && node --check risk-gate.js && node --check wallet.js && node --check paper-portfolio.js && node --check v2-simulator.js && node --check market-safety.js && node --check chain-config.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "viem": "^2.56.5"
  }
}
